# Handoff from 04-5-debug

When the debug routing is determined:

1. Recommend either `03-work` (if bugs were found) or `05-learn` (if clean).
2. Save/mention handoff-lite path under `.context/compound-engineering/handoffs/` using the shared `Handoff-lite template` in `skills/references/pipeline-config.md`.
3. Recommend new session only when cross-phase + health is heavy/critical, and include a copyable prompt.
