# Docsync Reference Guidelines

For document synchronization, follow the core instructions in `doc-sync/SKILL.md`.

Ensure:
- Minimal token usage in `AGENTS.md` (keep it under 400 lines).
- Both `README.md` and `AGENTS.md` are kept up-to-date with recent changes.
- Handoff instructions are strictly followed.
